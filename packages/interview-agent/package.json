{
  "name": "@aot/interview-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Claude Code agent that interviews a person and writes a World file.",
  "exports": {
    "./package.json": "./package.json",
    "./schema": "./schema/world.ts"
  },
  "scripts": {
    "validate": "node scripts/validate.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test 'schema/**/*.test.ts' 'scripts/**/*.test.ts'"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
